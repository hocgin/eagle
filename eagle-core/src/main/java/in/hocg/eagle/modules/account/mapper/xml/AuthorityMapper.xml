<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="in.hocg.eagle.modules.account.mapper.AuthorityMapper">

    <select id="search" resultType="in.hocg.eagle.modules.account.entity.Authority">
        SELECT ta.*
        FROM t_authority ta
        <where>
            <if test="qo.parentId != null">
                AND ta.parent_id = #{parentId}
            </if>
            <if test="qo.platform != null">
                AND ta.platform = #{qo.platform}
            </if>
        </where>
        ORDER BY sort DESC, created_at DESC
    </select>
    <select id="selectListByRegexTreePath" resultType="in.hocg.eagle.modules.account.entity.Authority">
        SELECT ta.*
        FROM t_authority ta
        WHERE ta.tree_path REGEXP #{regexTreePath}
    </select>
    <delete id="deleteListByRegexTreePath">
        DELETE FROM t_authority
        WHERE tree_path REGEXP #{regexTreePath}
    </delete>
</mapper>
