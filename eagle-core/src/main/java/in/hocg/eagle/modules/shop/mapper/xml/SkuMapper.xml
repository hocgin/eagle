<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="in.hocg.eagle.modules.shop.mapper.SkuMapper">

    <delete id="deleteAllByProductId">
        DELETE os FROM oms_sku os
        WHERE os.product_id = #{productId}
    </delete>
    <select id="selectListByProductId2" resultType="in.hocg.eagle.modules.shop.entity.Sku">
        SELECT os.* FROM oms_sku os
        WHERE os.product_id = #{productId}
    </select>

    <update id="subtractStock">
        UPDATE oms_sku
        SET stock = (stock - #{useStock})
        WHERE id = #{skuId} AND stock = #{preStock}
    </update>
    <update id="plusStock">
        UPDATE oms_sku
        SET stock = (stock + #{useStock})
        WHERE id = #{skuId} AND stock = #{preStock}
    </update>
</mapper>
